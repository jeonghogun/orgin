[pytest]
markers =
    heavy: marks tests that rely on external services or long-running workflows.
    e2e: marks end-to-end tests spanning multiple services.
    celery: marks tests that require a live Celery worker.
addopts = -m "not heavy"
